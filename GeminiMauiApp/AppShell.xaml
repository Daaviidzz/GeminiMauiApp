<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:GeminiMauiApp.Views"
       x:Class="GeminiMauiApp.AppShell"
       Shell.NavBarHasShadow="True"
       BackgroundColor="{StaticResource Primary}">

    <Shell.Resources>
        <Style TargetType="Shell" ApplyToDerivedTypes="True">
            <Setter Property="Shell.BackgroundColor" Value="{StaticResource Primary}" />
            <Setter Property="Shell.ForegroundColor" Value="White" />
            <Setter Property="Shell.TitleColor" Value="White" />
            <Setter Property="Shell.NavBarHasShadow" Value="True" />
        </Style>
    </Shell.Resources>

    <TabBar>
        <Tab Title="Chat" Icon="chat_icon.png">
            <ShellContent ContentTemplate="{DataTemplate views:ChatPage}" Route="ChatPage" />
        </Tab>
        <Tab Title="Ajustes" Icon="settings_icon.png">
            <ShellContent ContentTemplate="{DataTemplate views:SettingsPage}" Route="SettingsPage" />
        </Tab>
    </TabBar>
</Shell>
